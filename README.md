
  # Begin in IT

  This project is an interactive onboarding tool that walks new hires through how the SOS team works so they're ready to handle real tickets faster

  ## Running the code

  Run `npm i` to install the dependencies.
  Run `npm run dev` to start the development server.
  
